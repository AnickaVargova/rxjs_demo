{
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput & web-dev-server --node-resolve --open --watch"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "typescript": "^5.7.2"
  }
}
